[build]
  command = "npm run build"
  publish = "dist"

# Single-page app: serve index.html for any route so client-side routing works
# (and refreshing /closet, /planner, etc. doesn't 404).
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
